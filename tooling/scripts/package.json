{
	"name": "@repo/scripts",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"clean": "git clean -xdf .cache .turbo dist node_modules",
		"create:user": "dotenv -c -e ../../.env -- tsx ./src/create-user.ts",
		"migrate:agent-orgs": "dotenv -c -e ../../.env -- tsx ./src/assign-agents-to-organization.ts",
		"migrate:persona-backfill": "dotenv -c -e ../../.env -- tsx ./src/backfill-agent-personas.ts",
		"migrate:source-crypto": "dotenv -c -e ../../.env -- tsx ./src/reencrypt-source-configs.ts",
		"type-check": "tsc --noEmit"
	},
	"dependencies": {
		"@repo/api": "workspace:*",
		"@repo/auth": "workspace:*",
		"@repo/database": "workspace:*",
		"@repo/logs": "workspace:*"
	},
	"devDependencies": {
		"@repo/tsconfig": "workspace:*",
		"@types/node": "catalog:",
		"nanoid": "catalog:",
		"tsx": "catalog:",
		"typescript": "catalog:"
	}
}
